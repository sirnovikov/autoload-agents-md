---
description: Remove all managed .agents/skills/ symlinks, the gitignore block, and the manifest for this project.
---

Run the script at `${CLAUDE_PLUGIN_ROOT}/scripts/disable.sh` using the Bash tool and report its output verbatim.
