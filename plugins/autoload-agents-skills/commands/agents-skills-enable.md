---
description: Mirror .agents/skills/ into .claude/skills/ for this project (one-time opt-in, then auto-maintained each session).
---

Run the script at `${CLAUDE_PLUGIN_ROOT}/scripts/enable.sh` using the Bash tool and report its output verbatim.
